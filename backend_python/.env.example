# ======================================================================
# 《算一卦/Destiny》后端环境变量配置示例
# Backend Environment Variables Configuration Example
# ======================================================================

# ============== LLM 配置 | LLM Configuration ==============

# LLM 提供商 | LLM Provider
# 支持: openai, anthropic, azure, gemini, cohere, huggingface 等
LLM_PROVIDER=openai

# LLM 模型 | LLM Model
# OpenAI: gpt-4, gpt-3.5-turbo
# Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
# Azure: azure/gpt-4
LLM_MODEL=gpt-4

# LLM API密钥 | LLM API Key
# ⚠️ 必需配置 | Required
LLM_API_KEY=your_api_key_here

# LLM API基础URL | LLM API Base URL (可选 | Optional)
# 用于自定义API端点或使用代理
LLM_BASE_URL=

# LLM 上下文令牌数 | LLM Max Context Tokens
LLM_MAX_CONTEXT_TOKENS=8000

# LLM 温度参数 | LLM Temperature (0.0-2.0)
# 越高越随机，越低越确定
LLM_TEMPERATURE=0.7

# LLM 最大输出令牌数 | LLM Max Output Tokens
LLM_MAX_TOKENS=300

# ============== 旧版配置兼容 | Legacy Configuration ==============

# 默认模型 | Default Model (可选，优先使用 LLM_MODEL)
DEFAULT_LLM_MODEL=

# 备用模型 | Fallback Model (可选)
FALLBACK_LLM_MODEL=

# OpenAI API密钥 | OpenAI API Key (可选，优先使用 LLM_API_KEY)
OPENAI_API_KEY=

# Anthropic API密钥 | Anthropic API Key (可选)
ANTHROPIC_API_KEY=

# Azure OpenAI 配置 | Azure OpenAI Configuration (可选)
AZURE_API_KEY=
AZURE_API_BASE=
AZURE_API_VERSION=

# ============== 应用配置 | Application Configuration ==============

# 应用名称 | Application Name
APP_NAME=Destiny API

# 应用版本 | Application Version
APP_VERSION=1.0.0

# 调试模式 | Debug Mode (生产环境设置为 false)
DEBUG=false

# 服务器主机 | Server Host
HOST=0.0.0.0

# 服务器端口 | Server Port
PORT=8000

# ============== 数据库配置 | Database Configuration ==============

# 数据库URL | Database URL
# SQLite (默认): sqlite:///data/destiny.db
# PostgreSQL: postgresql://user:password@localhost:5432/destiny
DATABASE_URL=sqlite:///data/destiny.db

# ============== Redis配置 | Redis Configuration ==============

# Redis URL | Redis URL (可选)
REDIS_URL=redis://localhost:6379

# Redis 缓存TTL | Redis Cache TTL (秒)
REDIS_CACHE_TTL=3600

# ============== 安全配置 | Security Configuration ==============

# IP哈希盐值 | IP Hash Salt (建议修改为随机字符串)
IP_HASH_SALT=destiny_salt_2024_change_me

# 速率限制 | Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=10

# ============== CORS配置 | CORS Configuration ==============

# 允许的来源 | Allowed Origins (用逗号分隔)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","https://destiny.yourdomain.com"]

# ============== 日志配置 | Logging Configuration ==============

# 日志级别 | Log Level
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============== 使用说明 | Usage Instructions ==============
#
# 1. 复制此文件为 .env:
#    cp .env.example .env
#
# 2. 修改必需配置（标记为⚠️的项）:
#    - LLM_API_KEY: 必须配置你的 LLM API 密钥
#
# 3. 根据需要调整其他配置
#
# 4. 重启服务使配置生效
#
# ⚠️ 注意: 不要将 .env 文件提交到版本控制系统
